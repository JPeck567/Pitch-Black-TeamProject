package net.pitchblack.getenjoyment.helpers;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.badlogic.gdx.maps.tiled.TiledMap;

public class MapHandler {
	public static ArrayList<TiledMap> getGameMaps(int[] mapList) {
		// map creation
		List<Integer> initialMaps = Arrays.asList(ints);
		map = pbAssetManager.getAsset(PBAssetManager.map0);
		gameMaps = new ArrayList<Integer>(initialMaps);
	}
}
